(() => {
    'use strict';

    let solve = (input) => {
        if (!input) {
            throw new Error('Expected valid input.');
        }
    };

    module.exports = {
        solve: solve
    };
})();
